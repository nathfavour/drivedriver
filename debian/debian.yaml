flutter_app: 
  command: drivedriver
  arch: x64
  parent: /usr/local/lib
  nonInteractive: false

control:
  Package: drivedriver
  Version: 1.0.0
  Architecture: amd64
  Priority: optional
  Depends:
  Maintainer:
  Description: the file system wizard.
  
#options:
#  exec_out_dir: debian/packages